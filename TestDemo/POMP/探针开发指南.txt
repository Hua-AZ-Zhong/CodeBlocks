1、不加锁情况下的多进程同时操作一个文件
2、加锁情况下的多进程同时操作一个文件
其中，一个进程不释放锁，看另外一个进程是否报错无法写入

已确认fopen a+是每次都追加到文件最后


使用lib方式，弃用IPC队列，无需加锁
探针模块说明，设计文档，在完成开发后需要提供

探针任务可能所有产品重新排期到X97

接口疑问：
1、非定长字段不需要补空格
2、costtime的单位  毫秒ms
3、日志名IST_TO_POMP.YYYYMMDD.debug以及POSP_TO_POMP.YYYYMMDD.debug，位于cdt目录下
4、在切日的情况下，有可能同一笔交易的8行日志，分布在昨日和明日的两个日志文件中，经过确认，POMP系统可以处理
5、为空的字段需要使用竖线|隔开


模拟场景1
A程序第一次打印，不刷新
A程序第二次打印，刷新
B程序第一次打印，不刷新
B程序第二次打印，刷新
结论：各程序只刷新自己的缓冲区，A程序先写入缓冲区，然而不刷新，随后B程序写入自己的缓冲区并刷新，则日志先清空B程序的缓冲区

待解决场景
使用ftime()转换成localtime