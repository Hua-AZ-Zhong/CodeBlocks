/** 未初始化的全局变量，其默认值为0 **/
#include <stdio.h>

int externNum;

int main()
{
        int num;
        printf("externNum default value=[%d]\n", externNum);
        printf("num default value=[%d]\n", num);
}
